# Database
dataSource.url=${spring-repository-impl.dataSource.url}
dataSource.driverClassName=${spring-repository-impl.dataSource.driverClassName}
dataSource.username=${spring-repository-impl.dataSource.username}
dataSource.password=${spring-repository-impl.dataSource.password}

# Hibernate
hibernate.dialect=${spring-repository-impl.hibernate.dialect}
hibernate.hbm2ddl.auto=${spring-repository-impl.hibernate.hbm2ddl.auto}
hibernate.show_sql=${spring-repository-impl.hibernate.show_sql}

# Drop Definition
dropDefinition.testMarkerTableName=${spring-repository-impl.dropDefinition.testMarkerTableName}